package nl.hauntedmc.dungeons.command.config;

/**
 * Identifies which file a config command entry persists into.
 */
public enum DungeonConfigFile {
    /** The main dungeon config file. */
    MAIN,
    /** The branching generation settings file. */
    GENERATION
}
