/**
 * Listener implementations owned by a specific live dungeon instance.
 *
 * <p>Each instance receives its own listener set so world rules and gameplay restrictions are
 * scoped to that single copied world.</p>
 */
package nl.hauntedmc.dungeons.listener.instance;
